export function recordDelivery(store,input){const row={delivery_id:input.delivery_id||`DELIV_${Date.now()}`,disclosure_id:input.disclosure_id||null,entity_id:input.entity_id||null,recipient_id:input.recipient_id||null,recipient_role:input.recipient_role||"lp",delivery_status:input.delivery_status||"delivered",viewed:Boolean(input.viewed),acknowledged:Boolean(input.acknowledged),created_at:new Date().toISOString()};store.deliveries=store.deliveries||[];store.deliveries.push(row);return row;} export function recordDisclosureAcknowledgment(store,disclosureId,recipientId){const row={ack_id:`ACK_${Date.now()}`,disclosure_id:disclosureId,recipient_id:recipientId,acknowledged_at:new Date().toISOString()};store.acknowledgments=store.acknowledgments||[];store.acknowledgments.push(row);return row;}