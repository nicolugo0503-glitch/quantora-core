export function registerDisclosureRequirement(store, entityId, config){const row={requirement_id:config.requirement_id||`DISC_REQ_${Date.now()}`,entity_id:entityId,entity_type:config.entity_type||"product",cadence:config.cadence||"monthly",disclosure_type:config.disclosure_type||"factsheet",latest_period:config.latest_period||null,status:config.status||"active",created_at:new Date().toISOString()};store.requirements=store.requirements||[];store.requirements.push(row);return row;} export function listDisclosureRequirements(store){return (store.requirements||[]).slice();}